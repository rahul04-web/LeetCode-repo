//write a js fn to find the sum of square of first N natural number of array

const sumSquareOfArray=(start , end)=>{
    let sum=0;
    for(let i =start; i <=end;i++){
        sum
        sum+=Math.pow(i,2)
    }
    console.log(sum)
}
sumSquareOfArray(5,55)

